@model List<Scholarships_Web.Models.Scholarship>
<div style="display: flex; flex-wrap: wrap; gap: 20px">
    @for (int i = 0; i < Model.Count; ++i)
    {
        @Html.DisplayFor(m => m[i])
    }
</div>